// Notes.cpp - Project notes

/*
 * In TEST mode, the signals on the M1 float, hence it is best to force the signal
 * state on the shield otherwise it will impact switching from read <-> write operations.
 *
 * TODO's:
 * - May need to introduce latency to the video write as sometimes the M1 goes into 32
 *   character mode
 */
